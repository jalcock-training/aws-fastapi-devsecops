name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: Build & Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r app/requirements.txt

      - name: Run ruff linting
        run: |
          ruff check .

      - name: Run black formatting check
        run: |
          black --check .

      - name: Lint (placeholder)
        run: |
          echo "Linting will be added here"

      - name: Docker build (placeholder)
        run: |
          echo "Docker build will be added here"

      - name: Terraform validate (placeholder)
        run: |
          echo "Terraform validation will be added here"
